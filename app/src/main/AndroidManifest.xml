<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tranvanmanh.twamp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ResultActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DetailsActivity"
            android:parentActivityName=".ResultActivity" />
        <activity
            android:name=".ChartActivity"
            android:parentActivityName=".ResultActivity" />
        <activity
            android:name=".LatencyChart"
            android:parentActivityName=".ChartActivity" />
        <activity
            android:name=".IPDVChart"
            android:parentActivityName=".ChartActivity" />
        <activity android:name=".VoipChart" />
        <activity android:name=".VideoActivity" />
        <activity android:name=".DataAcitivity"></activity>
    </application>

</manifest>